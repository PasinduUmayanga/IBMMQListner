version: 1.0.{build}

image: Visual Studio 2022

configuration: Release
platform: Any CPU

cache:
  - packages -> **\packages.config

before_build:
  - nuget restore IBMMQListner.sln

build:
  project: IBMMQListner.sln
  verbosity: minimal

test: off
